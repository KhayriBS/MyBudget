<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyBudget"
        tools:targetApi="31">
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.auth.RegisterActivity" />
        <activity android:name=".ui.main.MainActivity" />
        <activity android:name=".ui.transaction.AddTransactionActivity" />
        <activity android:name=".ui.report.ReportsActivity" />
        <activity android:name=".ui.category.CategoryManagementActivity" />
        <activity android:name=".ui.budget.BudgetActivity" />
        <activity android:name=".ui.search.SearchActivity" />
        <activity android:name=".ui.saving.SavingsGoalsActivity" />
        <activity android:name=".ui.recurring.RecurringTransactionActivity" />
        <activity android:name=".ui.reminder.RemindersActivity" />
        <activity android:name=".ui.currency.CurrencyActivity" />
        <activity android:name=".ui.currency.CurrencySelectionActivity" />
        <activity android:name=".ui.member.MembersActivity" />
    </application>

</manifest>